<div class="jumbotron">
	<%= render 'partial/navbar' %>

  <div class="container">
   <div class="row">
     <div class="col-xs-10 col-xs-offset-1" style=" position: absolute;
  top: 50%;
  left: 50%;
  transform: translateY(-50%) translateX(-50%);
  -webkit- transform: translateY(-50%) translateX(-50%); text-align:center; margin-left:0%;">
       <h1 class="display-3" style="margin-top:-2%; text-align: center;
  color: #fff;
  text-shadow: 0 0 15px #FF6694;
  font-size: 45px;
  font-family: 'Hiragino Maru Gothic Pro','ヒラギノ丸ゴ Pro W4', sans-serif;"><br>レンカノ！</h1>
       <h3 class="col-sm-offset-3 col-sm-6" style="font-size:21px; text-align: center;
  color: #fff;
  text-shadow: 0 0 15px #FF6694;
  font-family: 'Hiragino Maru Gothic Pro','ヒラギノ丸ゴ Pro W4', sans-serif;">何気ない毎日に潤いを</h3>
     </div>
   </div> 
  </div>

  <!-- Search Form -->
  <div class="top-search-field">
    <%= form_tag search_path, method: :get do %>
      <div class="row">
        <div class="container">
          
          <div class="col-xs-10 col-md-11" style="padding-right: 0px">
            <%= text_field_tag :search, params[:search], placeholder: "レンカノ検索！", class: "form-control",id:"top-search-form" %>
          </div>

          <div class="col-xs-2 col-md-1" style="padding-left: 0px; ">
            <%= submit_tag "検索", class: "btn btn-danger" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

</div>

<div class="container">

<section id="services">
    <div class="container">
      <div class="row row-space-4" style="margin-top:15%;">
        <div class="col-xs-offset-1 col-xs-10 text-center">
          <h2 class="section-heading">レンカノ！とは<br></h2>
          <h3 class="" style="font-size:22px;">レンカノ！は彼女紹介動画を見て気になった子と気軽にデートできるレンタル彼女サービスです<br><br></h3>
        </div>
      </div>
      
    </div>
  </section>

<section id="services">
    <div class="container">
      <div class="row row-space-4" style="margin-top:15%;">
        <div class="col-xs-offset-1 col-xs-10 text-center">
          <h2 class="section-heading">レンカノ！使い方<br><br></h2>
          <h3 class="" style="font-size:22px;">彼女紹介動画を見て気に入った子のデート予約のボタンを押す！<br><br>名前、LineID、メアド、デート場所、デートの日付時間を入力して彼女に送信！<br><br>ラインで連絡が返ってきたら早速デート！料金はデート後に後払いです<br><br></h3>
        </div>
      </div>
      
    </div>
  </section>



<section id="portfolio" class="bg-light-gray row-space-top-4 row-space-4">
    <div class="container" style="">
      <div class="row row-space-2">
        <div class="col-xs-12 text-center">
          <h2 class="section-heading"><br>ピックアップ彼女紹介動画</h2>
          <h4 class="section-subheading"></h4>
        </div>
      </div>

    <div class="row" style="margin-top:7%; margin-bottom:7%;">
      <% @users.each do |user|%>
        <% if !user.listings.blank?%>
          <% if !user.listings[0].listing_title.blank? && !user.listings[0].listing_content.blank? && !user.listings[0].video.blank? && !user.listings[0].image.blank? && !user.listings[0].name.blank? && !user.listings[0].picup.blank? %>
            <div class="col-xs-offset-1 col-md-3 col-xs-10 portfolio-item row-space-2" style="">
              <%= link_to listing_path(user.listings[0]).first(8) do %>
                <%= link_to user.listings[0].video_url.to_s, id: "video" do %>
<%= image_tag user.listings[0].image.to_s, style: "box-shadow: 0 10px 10px #FF6694; background: #FF6694;display:block;opacity: 1;" %>
<i class="fa fa-caret-square-o-right" style="position: absolute;
  top: 30%;
  left: 50%;
  -ms-transform: translate(-50%,-50%);
  -webkit-transform: translate(-50%,-50%);
  transform: translate(-50%,-50%);
  margin:0;/*余計な隙間を除く*/
  padding:0;/*余計な隙間を除く*/
  color: white;/*アイコン色*/
  font-size: 100px;"></i>  
  <% end %>
  <% end %>
              <div class="portfolio-caption" style="font-size:17px;">
                <%= link_to listing_path(user.listings[0]) do %>
                  <% user.listings[0].name %>
                <% end %>
                <span style="font-size:14px;">さん</span>
                <div style="float:right;">
                <%= link_to listing_path(user.listings[0]) do %>
                  <% user.listings[0].age %>
                <% end %>
                <span style="font-size:14px;">歳</span></div>

                <div style="margin-top:5%">
                <%= link_to raw("この人を詳しく見てみる"),listing_path(user.listings[0]),class:"btn btn-block btn-social btn-lg btn-facebook", style:"width:220px; margin:auto; display:block; box-shadow: 2px 2px 3px 1px #FF6694; background-color:#FF6694;" %>
                </div>
              
              </div>
            </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div>
  </section>





  <section id="portfolio" class="bg-light-gray row-space-top-4 row-space-4">
    <div class="container">
      <div class="row row-space-2">
        <div class="col-xa-12 text-center">
          <h2 class="section-heading">新着彼女紹介動画</h2>
          <h4 class="section-subheading"></h4>
        </div>
      </div>

    <div class="row" style="margin-top:7%;">
      <% @users.order('updated_at desc').each do |user|%>
        <% if !user.listings.blank?%>
          <% if !user.listings[0].listing_title.blank? && !user.listings[0].listing_content.blank? && !user.listings[0].video.blank? && !user.listings[0].image.blank? && !user.listings[0].name.blank?%>
            <div class="col-xs-offset-1 col-md-3 col-xs-10 portfolio-item row-space-2" style="">
              <%= link_to listing_path(user.listings[0]).first(8) do %>
                <%= link_to user.listings[0].video_url.to_s, id: "video" do %>
<%= image_tag user.listings[0].image.to_s, style: "box-shadow: 0 10px 10px #FF6694; background: #FF6694;display:block;opacity: 1;" %>
<i class="fa fa-caret-square-o-right" style="position: absolute;
  top: 30%;
  left: 50%;
  -ms-transform: translate(-50%,-50%);
  -webkit-transform: translate(-50%,-50%);
  transform: translate(-50%,-50%);
  margin:0;/*余計な隙間を除く*/
  padding:0;/*余計な隙間を除く*/
  color: white;/*アイコン色*/
  font-size: 100px;"></i>  
  <% end %>
  <% end %>
              <div class="portfolio-caption" style="font-size:17px;">
                <%= link_to listing_path(user.listings[0]) do %>
                  <% user.listings[0].name %>
                <% end %>
                <span style="font-size:14px;">さん</span>
                <div style="float:right;">
                <%= link_to listing_path(user.listings[0]) do %>
                  <% user.listings[0].age %>
                <% end %>
                <span style="font-size:14px;">歳</span></div>

                <div style="margin-top:5%">
                <%= link_to raw("この人を詳しく見てみる"),listing_path(user.listings[0]),class:"btn btn-block btn-social btn-lg btn-facebook", style:"width:220px; margin:auto; display:block; box-shadow: 2px 2px 3px 1px #FF6694; background-color:#FF6694;" %>
                </div>
              
              </div>
            </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div>
    <%= paginate @users %>
  </section>

  
</div>

<%= render 'pages/footer' %>
